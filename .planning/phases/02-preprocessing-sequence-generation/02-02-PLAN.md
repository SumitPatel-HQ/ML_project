---
phase: 02-preprocessing-sequence-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/test_main_phase2.py
  - main.py
autonomous: true
requirements:
  - PREP-02
  - PREP-03
  - PREP-04
  - PREP-05
must_haves:
  truths:
    - "User can run the pipeline and see Phase 2 preprocessing proof output"
    - "User can inspect train/test shapes, date ranges, and a first normalized sequence preview from the CLI"
    - "User can verify preprocessing is connected to the main pipeline instead of existing as an isolated helper"
  artifacts:
    - path: "main.py"
      provides: "Pipeline entry point with Phase 2 preprocessing flow"
      min_lines: 70
      contains: "PHASE 2: Preprocessing & Sequence Generation"
    - path: "tests/test_main_phase2.py"
      provides: "Automated verification for main.py preprocessing integration"
      contains: "def test_main_prints_phase2_preprocessing_proof"
  key_links:
    - from: "main.py"
      to: "src.preprocessor"
      via: "imports preprocess and format_preprocessing_proof"
      pattern: "from src.preprocessor import"
    - from: "main.py"
      to: "src.data_loader"
      via: "passes loaded dataframe into preprocess"
      pattern: "preprocess\(df"
---

<objective>
Wire the tested preprocessing module into the CLI pipeline so Phase 2 outputs are visible and verifiable during normal execution.

Purpose: Phase 2 is only complete when the user can run the pipeline and inspect preprocessing evidence without digging into arrays manually.
Output: `main.py` updated for Phase 2 plus an automated integration test for the new CLI behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-preprocessing-sequence-generation/02-CONTEXT.md
@.planning/phases/02-preprocessing-sequence-generation/02-RESEARCH.md
@src/preprocessor.py
@main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing main-pipeline test for Phase 2 proof output</name>
  <files>tests/test_main_phase2.py</files>
  <read_first>
    - main.py
    - src/preprocessor.py
    - .planning/phases/02-preprocessing-sequence-generation/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `main.main()` prints the heading `PHASE 2: Preprocessing & Sequence Generation` after data loading.
    - Test 2: `main.main()` calls `preprocess(df)` with the dataframe returned by `load_data`.
    - Test 3: `main.main()` prints the string returned by `format_preprocessing_proof(...)`, including markers `X_train shape:` and `Train date range:`.
  </behavior>
  <action>
Create `tests/test_main_phase2.py` using pytest and monkeypatch/capsys.

Patch `setup_environment`, `load_data`, `display_statistics`, `check_missing_values`, `plot_price_history`, `preprocess`, and `format_preprocessing_proof` inside the imported `main` module so the test can focus on wiring instead of real file or model dependencies.

Assert that:

- `main.main()` prints `PHASE 2: Preprocessing & Sequence Generation`
- `preprocess` receives the loaded dataframe
- CLI output includes the exact proof markers `X_train shape:` and `Train date range:`

Keep the test fast and deterministic; no filesystem or dataset access.
  </action>
  <verify>
    <automated>pytest tests/test_main_phase2.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_main_phase2.py` contains `def test_main_prints_phase2_preprocessing_proof`
    - `tests/test_main_phase2.py` monkeypatches `preprocess` and `format_preprocessing_proof`
    - `tests/test_main_phase2.py` asserts CLI output includes `PHASE 2: Preprocessing & Sequence Generation`
    - `tests/test_main_phase2.py` asserts CLI output includes `X_train shape:` and `Train date range:`
  </acceptance_criteria>
  <done>
The intended CLI integration is captured in a failing test before main-pipeline changes are made.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update main.py to execute and print Phase 2 preprocessing proof</name>
  <files>main.py</files>
  <read_first>
    - main.py
    - src/preprocessor.py
    - src/data_loader.py
    - .planning/phases/02-preprocessing-sequence-generation/02-CONTEXT.md
  </read_first>
  <action>
Modify `main.py` so the standard pipeline now runs Phase 2 immediately after Phase 1 data loading/validation.

Make these exact changes:

- Add `from src.preprocessor import preprocess, format_preprocessing_proof` near the other imports.
- After Phase 1 loads `df`, displays stats, checks missing values, and creates the raw plot, add a new banner block:
  - `PHASE 2: Preprocessing & Sequence Generation`
- Call `bundle = preprocess(df)`.
- Call `proof = format_preprocessing_proof(bundle)`.
- Print the `proof` string directly so the user sees the concise proof set required by D-05 through D-09.
- Keep the final summary block and extend it with:
  - `X_train shape: {bundle['metadata']['X_train_shape']}`
  - `X_test shape: {bundle['metadata']['X_test_shape']}`
- Do not dump full tensors or large arrays; only print the concise proof formatter output.

Preserve the existing Phase 1 behavior and completion messaging while making Phase 2 part of the normal CLI flow.
  </action>
  <verify>
    <automated>pytest tests/test_main_phase2.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `main.py` contains `from src.preprocessor import preprocess, format_preprocessing_proof`
    - `main.py` contains `PHASE 2: Preprocessing & Sequence Generation`
    - `main.py` contains `bundle = preprocess(df)`
    - `main.py` contains `proof = format_preprocessing_proof(bundle)`
    - `main.py` prints the proof string
    - `main.py` final summary includes `X_train shape:` and `X_test shape:`
    - `pytest tests/test_main_phase2.py -x` exits 0
  </acceptance_criteria>
  <done>
Phase 2 preprocessing is connected to the user-facing pipeline and produces inspectable proof output during normal execution.
  </done>
</task>

</tasks>

<verification>
Run `pytest tests/test_main_phase2.py -x` to prove main.py imports the preprocessing module, executes it, and prints the required proof output markers.
</verification>

<success_criteria>
- [ ] `main.py` imports and runs `preprocess(df)`
- [ ] CLI output includes Phase 2 proof heading and concise proof block
- [ ] Proof output includes shapes and chronological range markers
- [ ] Full tensors are not dumped to the console
- [ ] Automated integration test passes via `pytest tests/test_main_phase2.py -x`
</success_criteria>

<output>
After completion, create `.planning/phases/02-preprocessing-sequence-generation/02-02-SUMMARY.md`
</output>
