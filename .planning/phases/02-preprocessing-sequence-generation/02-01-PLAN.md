---
phase: 02-preprocessing-sequence-generation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_preprocessor.py
  - src/preprocessor.py
autonomous: true
requirements:
  - PREP-01
  - PREP-02
  - PREP-03
  - PREP-04
  - PREP-05
must_haves:
  truths:
    - "User can generate normalized train/test tensors from Close-only price history"
    - "User can verify scaler fitting happened on training data only"
    - "User can confirm sequence tensors are shaped for LSTM input as (samples, 60, 1)"
    - "User gets explicit errors when preprocessing assumptions are violated"
  artifacts:
    - path: "src/preprocessor.py"
      provides: "Leakage-safe scaling, sequence generation, metadata, and proof formatting"
      min_lines: 120
      exports: ["preprocess", "format_preprocessing_proof"]
    - path: "tests/test_preprocessor.py"
      provides: "Automated verification for preprocessing contract and failure modes"
      contains: "def test_preprocess_returns_lstm_ready_tensors"
  key_links:
    - from: "src/preprocessor.py"
      to: "sklearn.preprocessing.MinMaxScaler"
      via: "fit_transform on train_raw and transform on test_input"
      pattern: "MinMaxScaler|fit_transform|transform"
    - from: "src/preprocessor.py"
      to: "main.py"
      via: "format_preprocessing_proof return string for CLI output"
      pattern: "def format_preprocessing_proof"
---

<objective>
Build the core preprocessing contract for Phase 2: Close-only extraction, train-only scaling, chronological splitting, 60-day sequence creation, and inspectable metadata.

Purpose: This is the correctness-critical foundation for later training work; if leakage prevention or tensor shaping is wrong here, every downstream phase becomes invalid.
Output: A tested `src/preprocessor.py` module plus focused preprocessing tests.
</objective>

<feature>
  <name>Leakage-safe preprocessing bundle</name>
  <files>src/preprocessor.py, tests/test_preprocessor.py</files>
  <behavior>
    - Valid dataframe input returns `X_train`, `y_train`, `X_test`, `y_test`, `scaler`, and `metadata` per D-01, D-02, D-03, and D-04.
    - `Close` is the only feature used per D-15 / PREP-01.
    - `MinMaxScaler(feature_range=(0, 1))` is fit on training rows only and reused for downstream transforms per D-16 / PREP-02.
    - Split stays chronological at 80/20 with no shuffling per D-17 / PREP-04.
    - Sliding windows use configurable default size `60` and output tensors shaped `(samples, 60, 1)` per D-18 / PREP-03 / PREP-05.
    - Test sequences include the final training window before test rows per D-19.
    - Missing `Close`, unresolved missing values, invalid config, too-short data, and inconsistent output raise descriptive `ValueError`s per D-10 through D-14.
    - `format_preprocessing_proof(...)` returns a concise report string containing shapes, split/date ranges, scaler range evidence, and first normalized sequence preview plus target per D-05 through D-09.
  </behavior>
</feature>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-preprocessing-sequence-generation/02-CONTEXT.md
@.planning/phases/02-preprocessing-sequence-generation/02-RESEARCH.md
@.docs/TDD_StockPrediction_LSTM.md
@.docs/SRS_StockPrediction_LSTM.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing preprocessing contract tests</name>
  <files>tests/test_preprocessor.py</files>
  <read_first>
    - .planning/phases/02-preprocessing-sequence-generation/02-CONTEXT.md
    - .planning/phases/02-preprocessing-sequence-generation/02-RESEARCH.md
    - .planning/REQUIREMENTS.md
    - .docs/TDD_StockPrediction_LSTM.md
  </read_first>
  <behavior>
    - Test 1: `preprocess(...)` returns a mapping with keys `X_train`, `y_train`, `X_test`, `y_test`, `scaler`, and `metadata`.
    - Test 2: `X_train` and `X_test` have trailing shape `(60, 1)` and targets remain 1D arrays.
    - Test 3: metadata records `feature_name == "Close"`, `window_size == 60`, split/date ranges, sequence counts, and tensor shapes.
    - Test 4: first test sequence starts with the last 60 raw training rows transformed by the training-fitted scaler, proving D-19 continuity.
    - Test 5: `format_preprocessing_proof(...)` contains exact markers `X_train shape:`, `y_train shape:`, `X_test shape:`, `y_test shape:`, `Train date range:`, `Test date range:`, `First normalized sequence preview:`, and `First target:`.
    - Test 6: missing `Close`, unresolved missing values, invalid `train_split`, invalid `window_size`, and too-short datasets each raise `ValueError` with descriptive phrases.
  </behavior>
  <action>
Create `tests/test_preprocessor.py` with pytest coverage for the full Phase 2 contract.

Use a small synthetic pandas DataFrame with a datetime index and deterministic `Close` values so the tests can assert exact split behavior. Write tests for the locked decisions per D-01 through D-19, especially:

- return structure contains `X_train`, `y_train`, `X_test`, `y_test`, `scaler`, `metadata` (D-01)
- metadata includes split/date context, feature/window info, sequence counts, and tensor shapes (D-02, D-03, D-04)
- proof formatter output includes concise proof markers (D-05 through D-09)
- failure cases raise `ValueError` instead of warnings or silent fixes (D-10 through D-14)
- only `Close` is used, scaler is train-only fitted, split is chronological 80/20, and window size defaults to 60 (D-15 through D-18)
- test continuity includes the last training window before test rows (D-19)

Import from `src.preprocessor` using the intended public API names `preprocess` and `format_preprocessing_proof`. Keep fixture generation inside this file unless a shared fixture becomes necessary.
  </action>
  <verify>
    <automated>pytest tests/test_preprocessor.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_preprocessor.py` contains `def test_preprocess_returns_lstm_ready_tensors`
    - `tests/test_preprocessor.py` contains `def test_preprocess_preserves_test_continuity_with_last_train_window`
    - `tests/test_preprocessor.py` contains `def test_format_preprocessing_proof_includes_required_markers`
    - `tests/test_preprocessor.py` contains assertions for keys `X_train`, `y_train`, `X_test`, `y_test`, `scaler`, and `metadata`
    - `tests/test_preprocessor.py` asserts exact proof markers `X_train shape:` and `First normalized sequence preview:`
    - `tests/test_preprocessor.py` includes at least one `with pytest.raises(ValueError, match=...)` for each invalid-input category
  </acceptance_criteria>
  <done>
Failing tests fully describe the preprocessing contract before implementation, covering normal behavior, proof output, and explicit failure modes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement preprocessing module to satisfy the tests</name>
  <files>src/preprocessor.py</files>
  <read_first>
    - tests/test_preprocessor.py
    - src/config.py
    - .planning/phases/02-preprocessing-sequence-generation/02-CONTEXT.md
    - .planning/phases/02-preprocessing-sequence-generation/02-RESEARCH.md
    - .docs/TDD_StockPrediction_LSTM.md
  </read_first>
  <behavior>
    - `preprocess(...)` accepts the Phase 1 dataframe and returns a structured bundle that tests can consume directly.
    - Internal helpers validate inputs before scaling or sequencing.
    - `format_preprocessing_proof(...)` returns a concise multiline string instead of printing directly.
  </behavior>
  <action>
Create `src/preprocessor.py` and implement the production code required by the tests.

Use these exact public functions:

```python
def preprocess(df, cfg=None):
    ...

def format_preprocessing_proof(bundle):
    ...
```

Implementation requirements:

- Read defaults from `src.config` using `TARGET_COLUMN`, `TRAIN_SPLIT`, `SEQUENCE_LENGTH`, and `NORMALIZE_RANGE`.
- Support optional `cfg` overrides for `feature_col`, `train_split`, `window_size`, and `normalize_range` without mutating module-level config.
- Extract `df[[feature_col]].values` and reject missing `feature_col` or unresolved missing values with `ValueError` per D-11.
- Validate `0 &lt; train_split &lt; 1`, `window_size &gt;= 1`, and enough rows to produce at least one train and one test sequence; raise `ValueError` per D-12 and D-13 otherwise.
- Compute `split_index = int(len(data) * train_split)` and preserve chronological order with no shuffling per D-17.
- Fit `MinMaxScaler(feature_range=normalize_range)` on `train_raw` only per D-16.
- Build `test_input` with `np.concatenate([train_raw[-window_size:], test_raw])` per D-19, then transform it with the already-fitted scaler.
- Use a private helper like `_create_sequences(data, window_size)` to build windows and targets; reshape feature tensors to `(-1, window_size, 1)` per PREP-05.
- Return a mapping with keys exactly `X_train`, `y_train`, `X_test`, `y_test`, `scaler`, and `metadata` per D-01.
- Populate `metadata` with exact keys: `feature_name`, `window_size`, `train_split`, `split_index`, `train_date_range`, `test_date_range`, `train_sequence_count`, `test_sequence_count`, `X_train_shape`, `y_train_shape`, `X_test_shape`, `y_test_shape`, `train_scaled_min`, `train_scaled_max`, `test_scaled_min`, `test_scaled_max`, `first_sequence_preview`, `first_sequence_target` per D-02, D-03, and D-04.
- Implement `format_preprocessing_proof(bundle)` to return a concise multiline string containing exact labels `X_train shape:`, `y_train shape:`, `X_test shape:`, `y_test shape:`, `Train date range:`, `Test date range:`, `Feature:`, `Window size:`, `Scaled train range:`, `First normalized sequence preview:`, and `First target:` per D-05 through D-09.
- Raise `ValueError` for scaling/sequencing mismatches that would create inconsistent shapes per D-14.

Do not print from `preprocess(...)`. Keep proof formatting separated in `format_preprocessing_proof(...)` so `main.py` can own console output later.
  </action>
  <verify>
    <automated>pytest tests/test_preprocessor.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/preprocessor.py` contains `def preprocess(df, cfg=None):`
    - `src/preprocessor.py` contains `def format_preprocessing_proof(bundle):`
    - `src/preprocessor.py` imports `MinMaxScaler` from `sklearn.preprocessing`
    - `src/preprocessor.py` contains `np.concatenate([train_raw[-window_size:], test_raw])`
    - `src/preprocessor.py` reshapes both feature tensors with `window_size` and trailing dimension `1`
    - `src/preprocessor.py` returns keys `X_train`, `y_train`, `X_test`, `y_test`, `scaler`, `metadata`
    - `src/preprocessor.py` metadata contains `train_date_range`, `test_date_range`, `X_train_shape`, and `first_sequence_preview`
    - `pytest tests/test_preprocessor.py -x` exits 0
  </acceptance_criteria>
  <done>
The Phase 2 preprocessing engine is implemented, validated, and ready for later training and CLI wiring.
  </done>
</task>

</tasks>

<verification>
Run `pytest tests/test_preprocessor.py -x` and confirm the module returns leakage-safe tensors, metadata, and proof text while rejecting invalid inputs.
</verification>

<success_criteria>
- [ ] `src/preprocessor.py` exists with `preprocess` and `format_preprocessing_proof`
- [ ] Train/test split is chronological and uses no shuffling
- [ ] Scaler is fit on training data only
- [ ] `X_train` and `X_test` are shaped `(samples, 60, 1)` by default
- [ ] Metadata exposes split context, shapes, counts, and sequence preview
- [ ] Invalid preprocessing inputs fail fast with descriptive `ValueError`s
- [ ] Automated tests pass via `pytest tests/test_preprocessor.py -x`
</success_criteria>

<output>
After completion, create `.planning/phases/02-preprocessing-sequence-generation/02-01-SUMMARY.md`
</output>
