---
phase: 03-model-architecture-training
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_model.py
  - src/model.py
autonomous: true
requirements:
  - MODEL-01
  - MODEL-02
  - MODEL-03
  - MODEL-04
  - MODEL-05
must_haves:
  truths:
    - "A Phase 3 model builder returns a compiled stacked LSTM with the exact required layer order."
    - "TensorFlow absence fails only when model creation is invoked, not when unrelated modules are imported."
    - "The model contract is inspectable from automated tests without running full training."
  artifacts:
    - path: "src/model.py"
      provides: "Localized TensorFlow model builder and summary helper"
      exports: ["build_model", "format_model_summary"]
    - path: "tests/test_model.py"
      provides: "Contract tests for architecture, compilation, and runtime import behavior"
  key_links:
    - from: "src/model.py"
      to: "src/config.py"
      via: "centralized architecture constants"
      pattern: "from src\.config import"
    - from: "tests/test_model.py"
      to: "src/model.py"
      via: "public API import and contract assertions"
      pattern: "build_model|format_model_summary"
---

<objective>
Define and prove the Phase 3 model-building contract before trainer or CLI wiring depends on it.

Purpose: Lock the exact LSTM architecture and localized TensorFlow import behavior early so later training work builds against a stable interface.
Output: A tested `src/model.py` module that creates the required compiled stacked LSTM model and exposes summary text for CLI use.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-model-architecture-training/03-CONTEXT.md
@.planning/phases/03-model-architecture-training/03-RESEARCH.md
@.planning/phases/02-preprocessing-sequence-generation/02-01-SUMMARY.md
@src/config.py
@src/preprocessor.py

<interfaces>
From `src/config.py`:
```python
SEQUENCE_LENGTH = 60
LSTM_UNITS = 64
DROPOUT_RATE = 0.2
DENSE_UNITS = 32
LEARNING_RATE = 0.001
RANDOM_SEED = 42
```

From `src.preprocessor.preprocess()`:
```python
return {
    "X_train": X_train,
    "y_train": y_train,
    "X_test": X_test,
    "y_test": y_test,
    "scaler": scaler,
    "metadata": metadata,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing model contract tests</name>
  <files>tests/test_model.py</files>
  <read_first>
    - src/config.py
    - src/preprocessor.py
    - tests/test_preprocessor.py
  </read_first>
  <behavior>
    - Test 1: `build_model()` returns a model with layer types `LSTM, Dropout, LSTM, Dropout, Dense, Dense` and units/rates matching `64, 0.2, 64, 0.2, 32, 1`.
    - Test 2: the compiled optimizer is Adam with learning rate `0.001` and the loss resolves to mean squared error.
    - Test 3: `format_model_summary(model)` returns text containing `LSTM`, `Dropout`, and `Dense` markers for CLI proof.
    - Test 4: if TensorFlow cannot be imported, importing `src.model` still succeeds but calling `build_model()` raises a clear runtime error per D-06 and D-07.
  </behavior>
  <action>Create `tests/test_model.py` with import-friendly contract coverage for `src.model`. Use stubs or monkeypatching where needed so the missing-TensorFlow path is testable without requiring a broken environment. Assert the exact architecture and compile settings from `MODEL-01` through `MODEL-05`, and reference D-06/D-07 in test names or comments so localized import behavior is traceable.</action>
  <acceptance_criteria>
    - `tests/test_model.py` contains `def test_build_model_returns_required_stacked_lstm_architecture`.
    - `tests/test_model.py` contains assertions for `LSTM`, `Dropout`, `Dense`, `0.2`, `64`, `32`, and `0.001`.
    - `tests/test_model.py` contains a test covering runtime failure when TensorFlow is unavailable.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_model.py -x</automated>
  </verify>
  <done>Failing tests describe the exact public contract for model construction, summary formatting, and lazy TensorFlow error behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the localized TensorFlow model module</name>
  <files>src/model.py</files>
  <read_first>
    - src/config.py
    - src/preprocessor.py
    - tests/test_model.py
    - src/utils.py
  </read_first>
  <behavior>
    - `build_model(input_shape=(60, 1))` returns a compiled `Sequential` model matching the required architecture.
    - `format_model_summary(model)` returns stable summary lines suitable for printing from `main.py`.
    - Importing `src.model` does not require TensorFlow until `build_model()` or summary capture helpers run.
  </behavior>
  <action>Create `src/model.py` with a small runtime helper that imports `tensorflow`/`tensorflow.keras` only inside callable code; if unavailable, raise `RuntimeError("TensorFlow is required to build or train the Phase 3 LSTM model.")` per D-06 and D-07. Implement `build_model(input_shape=None, cfg=None)` using config defaults `SEQUENCE_LENGTH=60`, `LSTM_UNITS=64`, `DROPOUT_RATE=0.2`, `DENSE_UNITS=32`, and `LEARNING_RATE=0.001`. Build `Sequential([LSTM(64, return_sequences=True, input_shape=(60, 1)), Dropout(0.2), LSTM(64, return_sequences=False), Dropout(0.2), Dense(32, activation="relu"), Dense(1)])`, compile with `Adam(learning_rate=0.001)` and `loss="mean_squared_error"`, and add `format_model_summary(model)` that captures `model.summary()` into newline-joined text for later CLI printing.</action>
  <acceptance_criteria>
    - `src/model.py` contains `def build_model(` and `def format_model_summary(`.
    - `src/model.py` contains `LSTM(` twice, `Dropout(` twice, `Dense(32` once, and `Dense(1` once.
    - `src/model.py` contains `learning_rate=0.001` and `mean_squared_error`.
    - `src/model.py` does not import TensorFlow at module top level before helper invocation.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_model.py -x</automated>
  </verify>
  <done>`src.model` provides a tested architecture contract that downstream trainer and CLI work can import directly.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/test_model.py -x` and confirm the green suite proves exact layer structure, compile configuration, and lazy TensorFlow failure behavior.</verification>

<success_criteria>
- `src/model.py` exists with the exact Phase 3 architecture.
- Model creation fails clearly only when invoked without TensorFlow.
- Summary text is available for later CLI proof output.
</success_criteria>

<output>
After completion, create `.planning/phases/03-model-architecture-training/03-01-SUMMARY.md`
</output>
