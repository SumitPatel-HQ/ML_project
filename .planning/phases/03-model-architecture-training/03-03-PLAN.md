---
phase: 03-model-architecture-training
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - tests/test_main_phase3.py
  - main.py
autonomous: true
requirements:
  - TRAIN-05
must_haves:
  truths:
    - "The CLI shows a Phase 3 section with model summary, epoch-level training output, and a concise final proof summary."
    - "The final Phase 3 console proof highlights best epoch, EarlyStopping result, and the saved model path."
    - "Main pipeline wiring consumes the structured training bundle rather than reconstructing training state manually."
  artifacts:
    - path: "main.py"
      provides: "Phase 3 pipeline orchestration and user-facing proof output"
    - path: "tests/test_main_phase3.py"
      provides: "CLI wiring test for model build, train call, and summary output"
  key_links:
    - from: "main.py"
      to: "src.model.build_model"
      via: "Phase 3 model creation"
      pattern: "build_model\("
    - from: "main.py"
      to: "src.trainer.train_model"
      via: "Phase 3 training invocation"
      pattern: "train_model\("
    - from: "main.py"
      to: "src.trainer.format_training_summary"
      via: "CLI proof output"
      pattern: "format_training_summary\("
---

<objective>
Wire the Phase 3 model and trainer into the CLI pipeline so training progress and proof output are visible from `python main.py`.

Purpose: Make the completed training work user-observable and ready for downstream evaluation work without forcing the user to inspect internal modules.
Output: A `main.py` flow and CLI integration test that show Phase 3 banners, model summary text, and the locked final proof lines.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-model-architecture-training/03-CONTEXT.md
@.planning/phases/03-model-architecture-training/03-RESEARCH.md
@.planning/phases/02-preprocessing-sequence-generation/02-02-SUMMARY.md
@main.py
@src/model.py
@src/trainer.py
@src/preprocessor.py

<interfaces>
From `src.model`:
```python
def build_model(input_shape=None, cfg=None): ...
def format_model_summary(model): ...
```

From `src.trainer`:
```python
def train_model(model, preprocessing_bundle, cfg=None): ...
def format_training_summary(training_bundle): ...
```

From `main.py` current pattern:
```python
bundle = preprocess(df)
proof = format_preprocessing_proof(bundle)
print(proof)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the failing Phase 3 CLI integration contract</name>
  <files>tests/test_main_phase3.py</files>
  <read_first>
    - main.py
    - tests/test_main_phase2.py
    - src/model.py
    - src/trainer.py
  </read_first>
  <behavior>
    - Test 1: `main.main()` prints `PHASE 3: Model Architecture & Training` after preprocessing.
    - Test 2: `main.main()` calls `build_model(...)`, `format_model_summary(model)`, `train_model(model, bundle)`, and `format_training_summary(result)`.
    - Test 3: output contains the proof strings for model summary, best epoch, EarlyStopping status, and `output/best_model.h5` per D-01, D-02, and D-03.
  </behavior>
  <action>Create `tests/test_main_phase3.py` by following the monkeypatch-heavy style already used in `tests/test_main_phase2.py`. Fake the model builder and trainer outputs so the contract stays fast and deterministic. Assert exact output markers: `PHASE 3: Model Architecture & Training`, `best_model.h5`, `Best epoch:`, and `EarlyStopping`.</action>
  <acceptance_criteria>
    - `tests/test_main_phase3.py` contains `def test_main_prints_phase3_training_proof`.
    - `tests/test_main_phase3.py` asserts `PHASE 3: Model Architecture & Training`, `Best epoch:`, `EarlyStopping`, and `best_model.h5`.
    - `tests/test_main_phase3.py` verifies `train_model` receives the preprocessing bundle returned by `preprocess`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_main_phase3.py -x</automated>
  </verify>
  <done>The CLI contract clearly defines how Phase 3 should appear and which model/trainer APIs `main.py` must call.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Phase 3 model building and training into main.py</name>
  <files>main.py</files>
  <read_first>
    - main.py
    - src/model.py
    - src/trainer.py
    - src/preprocessor.py
    - tests/test_main_phase3.py
  </read_first>
  <action>Update `main.py` to import `build_model`, `format_model_summary`, `train_model`, and `format_training_summary`. After Phase 2 preprocessing completes, print a new banner `PHASE 3: Model Architecture & Training`, build the model using the sequence shape derived from `bundle["X_train"].shape[1:]`, print the captured model summary text, run `train_model(model, bundle)`, and print the summary returned by `format_training_summary(training_result)`. Preserve the existing proof-first CLI style from earlier phases and update the final completion block so it includes `Model: output/best_model.h5` and `Training sidecar:` with the returned sidecar path. Keep all training state access through the structured training bundle per D-08/D-09/D-10; do not reconstruct history manually in `main.py`.</action>
  <acceptance_criteria>
    - `main.py` contains `PHASE 3: Model Architecture & Training`.
    - `main.py` imports and calls `build_model`, `format_model_summary`, `train_model`, and `format_training_summary`.
    - `main.py` prints `Model: output/best_model.h5` in the final summary block.
    - `main.py` uses `bundle["X_train"].shape[1:]` or equivalent structured bundle data to define model input shape.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_main_phase3.py -x</automated>
  </verify>
  <done>Running the CLI will visibly include Phase 3 model construction and training proof output, ready for later Phase 4 evaluation wiring.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/test_main_phase3.py -x` and confirm Phase 3 wiring appears in CLI output and uses only the structured training bundle for proof.</verification>

<success_criteria>
- `main.py` exposes Phase 3 training behavior to the user.
- Best-epoch and saved-model proof markers are present in CLI output.
- Phase 3 integration uses the stable model/trainer interfaces from earlier waves.
</success_criteria>

<output>
After completion, create `.planning/phases/03-model-architecture-training/03-03-SUMMARY.md`
</output>
