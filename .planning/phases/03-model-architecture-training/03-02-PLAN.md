---
phase: 03-model-architecture-training
plan: 02
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/test_trainer.py
  - src/config.py
  - src/trainer.py
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-02
  - TRAIN-03
  - TRAIN-04
  - TRAIN-05
must_haves:
  truths:
    - "Training returns a structured bundle instead of a loose tuple or saved-model-only handoff."
    - "Training is configured with validation split, EarlyStopping, and ModelCheckpoint using the exact Phase 3 defaults."
    - "The best model path and lightweight training-history sidecar are saved and reported for later phases."
  artifacts:
    - path: "src/trainer.py"
      provides: "Training orchestration, callback setup, sidecar persistence, and CLI summary formatter"
      exports: ["train_model", "format_training_summary"]
    - path: "tests/test_trainer.py"
      provides: "Contract tests for callback wiring, bundle shape, sidecar metadata, and lazy TensorFlow failure behavior"
    - path: "src/config.py"
      provides: "Training sidecar filename/path constants used by trainer"
  key_links:
    - from: "src/trainer.py"
      to: "src/model.py"
      via: "trained model input"
      pattern: "train_model\(model, preprocessing_bundle"
    - from: "src/trainer.py"
      to: "output/best_model.h5"
      via: "ModelCheckpoint filepath"
      pattern: "best_model\.h5"
    - from: "src/trainer.py"
      to: "output/"
      via: "JSON sidecar write"
      pattern: "json|training_history"
---

<objective>
Turn the Phase 3 trainer into a tested contract that runs Keras fit with the exact callbacks, saves the locked artifacts, and returns a Phase-4-ready bundle.

Purpose: Keep training behavior deterministic and inspectable while honoring the user’s locked reporting and handoff decisions.
Output: A tested trainer module that saves `output/best_model.h5`, writes a lightweight JSON sidecar, and returns a structured training-result bundle.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-model-architecture-training/03-CONTEXT.md
@.planning/phases/03-model-architecture-training/03-RESEARCH.md
@.planning/phases/03-model-architecture-training/03-01-PLAN.md
@src/config.py
@src/model.py
@src/preprocessor.py
@main.py

<interfaces>
From `src.model` (created by 03-01):
```python
def build_model(input_shape=None, cfg=None): ...
def format_model_summary(model): ...
```

From `src.preprocessor.preprocess()` output:
```python
bundle["X_train"]
bundle["y_train"]
bundle["X_test"]
bundle["y_test"]
bundle["metadata"]
```

From `src/config.py`:
```python
BATCH_SIZE = 32
EPOCHS = 100
VALIDATION_SPLIT = 0.1
EARLY_STOPPING_PATIENCE = 10
EARLY_STOPPING_MONITOR = "val_loss"
MODEL_FILE = "best_model.h5"
OUTPUT_DIR = "output"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing trainer contract tests</name>
  <files>tests/test_trainer.py</files>
  <read_first>
    - src/config.py
    - src/model.py
    - src/preprocessor.py
    - tests/test_preprocessor.py
    - tests/test_main_phase2.py
  </read_first>
  <behavior>
    - Test 1: `train_model(...)` calls `model.fit()` with `epochs=100`, `batch_size=32`, `validation_split=0.1`, callbacks, and per-epoch verbosity satisfying D-01.
    - Test 2: the returned bundle includes `model`, `history`, `checkpoint_path`, `sidecar_path`, and `metadata` per D-08/D-09/D-10.
    - Test 3: metadata includes `epochs_run`, `best_epoch`, `best_val_loss`, `stopped_epoch`, config values used, and checkpoint path per D-05.
    - Test 4: `format_training_summary(bundle)` contains the best epoch, EarlyStopping status, saved model path, and sidecar path per D-02/D-03/D-04.
    - Test 5: TensorFlow absence raises a clear runtime error only when trainer functionality is invoked per D-06/D-07.
  </behavior>
  <action>Create `tests/test_trainer.py` using a fake model object with a controllable `fit()` method and fake callback classes so the suite stays lightweight. Assert the exact training defaults from `TRAIN-01` through `TRAIN-05`, the locked save path `output/best_model.h5` from D-03, and a lightweight JSON sidecar path under `output/` from D-04. Make the expected bundle keys explicit so Phase 4 can rely on them without discovery.</action>
  <acceptance_criteria>
    - `tests/test_trainer.py` contains `def test_train_model_returns_structured_training_bundle`.
    - `tests/test_trainer.py` asserts `epochs=100`, `batch_size=32`, `validation_split=0.1`, `val_loss`, and `best_model.h5`.
    - `tests/test_trainer.py` contains coverage for `format_training_summary` and lazy TensorFlow failure behavior.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_trainer.py -x</automated>
  </verify>
  <done>Failing tests capture the training API, callback config, artifact paths, summary proof, and structured handoff bundle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement trainer bundle, callbacks, and lightweight sidecar</name>
  <files>src/config.py, src/trainer.py</files>
  <read_first>
    - src/config.py
    - src/model.py
    - tests/test_trainer.py
    - main.py
    - src/utils.py
  </read_first>
  <behavior>
    - `train_model(model, preprocessing_bundle, cfg=None)` saves the best model, returns the training bundle, and writes a small JSON sidecar.
    - `format_training_summary(bundle)` emits CLI-ready proof lines focused on best epoch, EarlyStopping, and saved artifact paths.
    - Trainer imports TensorFlow/Keras only inside runtime helpers and raises a clear runtime error if unavailable.
  </behavior>
  <action>Update `src/config.py` to add explicit training sidecar constants such as `TRAINING_SIDECAR_FILE = "training_history.json"`. Create `src/trainer.py` with runtime-local TensorFlow imports, `train_model(model, preprocessing_bundle, cfg=None)`, helper(s) for callback creation, and helper(s) that extract metadata from `History.history`. Call `model.fit(preprocessing_bundle["X_train"], preprocessing_bundle["y_train"], epochs=100, batch_size=32, validation_split=0.1, callbacks=[EarlyStopping(monitor="val_loss", patience=10, restore_best_weights=True, verbose=1), ModelCheckpoint(filepath="output/best_model.h5", monitor="val_loss", save_best_only=True, verbose=1)], verbose=2)`. Save a lightweight JSON sidecar under `output/training_history.json` containing `loss`, `val_loss`, `epochs_run`, `best_epoch`, `best_val_loss`, `stopped_epoch`, `early_stopped`, `batch_size`, `validation_split`, `patience`, `monitor`, `checkpoint_path`, and the preprocessing tensor shapes from the input bundle metadata. Return `{ "model": model, "history": history.history, "checkpoint_path": "output/best_model.h5", "sidecar_path": "output/training_history.json", "metadata": metadata }`. Implement `format_training_summary(bundle)` so it prints best epoch, best validation loss, whether EarlyStopping triggered, epochs run, checkpoint path, and sidecar path per D-01 through D-05 and D-08 through D-10.</action>
  <acceptance_criteria>
    - `src/config.py` contains `TRAINING_SIDECAR_FILE`.
    - `src/trainer.py` contains `def train_model(` and `def format_training_summary(`.
    - `src/trainer.py` contains `validation_split=0.1`, `epochs=100`, `batch_size=32`, `monitor="val_loss"`, `patience=10`, and `output/best_model.h5`.
    - `src/trainer.py` writes `training_history.json` and returns a bundle with `model`, `history`, `checkpoint_path`, `sidecar_path`, and `metadata` keys.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_trainer.py -x</automated>
  </verify>
  <done>Phase 3 training behavior is fully specified in code: callbacks, live epoch output, proof summary, saved model, sidecar metadata, and structured handoff bundle.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/test_trainer.py -x` and confirm callback configuration, metadata extraction, bundle shape, and save-path behavior all pass.</verification>

<success_criteria>
- `src.trainer` returns a Phase-4-ready bundle instead of a tuple.
- `output/best_model.h5` is the checkpoint target.
- A lightweight training-history JSON sidecar is planned and verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/03-model-architecture-training/03-02-SUMMARY.md`
</output>
