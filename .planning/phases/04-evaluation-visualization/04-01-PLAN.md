---
phase: 04-evaluation-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/evaluator.py
  - tests/test_evaluator.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-06
must_haves:
  truths:
    - User can generate predictions on the test set from the trained Phase 3 model.
    - User sees RMSE and MAPE computed on inverse-transformed USD prices.
    - User gets a saved `output/metrics.json` artifact showing metric thresholds and pass/fail state.
    - User can prove the saved `output/best_model.h5` model can be reloaded for inference without retraining.
  artifacts:
    - path: src/evaluator.py
      provides: Evaluation helpers for inference, inverse transform, metrics, metrics artifact writing, and reload smoke test.
      exports:
        - evaluate_model
        - reload_saved_model_smoke_test
        - format_evaluation_summary
    - path: tests/test_evaluator.py
      provides: Contract coverage for prediction generation, inverse transform, metrics, artifact writing, and reuse proof.
    - path: src/config.py
      provides: Stable evaluation constants for metrics artifact naming and threshold comparison.
      contains: METRICS_FILE
  key_links:
    - from: src/evaluator.py
      to: src.trainer.train_model output
      via: training_result['model'] and training_result['checkpoint_path']
      pattern: training_result
    - from: src/evaluator.py
      to: src.preprocessor.preprocess output
      via: preprocessing_bundle['X_test'], ['y_test'], ['scaler']
      pattern: preprocessing_bundle
    - from: src/evaluator.py
      to: output/metrics.json
      via: JSON serialization of metrics and threshold status
      pattern: metrics.json
---

<objective>
Create the reusable Phase 4 evaluation contract so the pipeline can generate predictions, inverse-transform them to USD, compute RMSE/MAPE, persist a lightweight metrics artifact, and prove saved-model reuse.

Purpose: This is the Phase 4 foundation consumed by the CLI and plot wiring.
Output: `src/evaluator.py`, evaluation config constants, and passing contract tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-evaluation-visualization/04-CONTEXT.md
@.planning/phases/04-evaluation-visualization/04-RESEARCH.md
@.planning/phases/03-model-architecture-training/03-02-SUMMARY.md
@.planning/phases/03-model-architecture-training/03-03-SUMMARY.md
@src/config.py
@src/preprocessor.py
@src/trainer.py
@src/model.py

<interfaces>
From `src.preprocessor.py`:
```python
def preprocess(df, cfg=None):
    return {
        "X_train": X_train,
        "y_train": y_train,
        "X_test": X_test,
        "y_test": y_test,
        "scaler": scaler,
        "metadata": metadata,
    }
```

From `src.trainer.py`:
```python
def train_model(model, preprocessing_bundle, cfg=None):
    return {
        "model": model,
        "history": history.history,
        "checkpoint_path": checkpoint_path,
        "sidecar_path": sidecar_path,
        "metadata": metadata,
    }
```

From `src.model.py`:
```python
TF_REQUIRED_ERROR = "TensorFlow is required to build or train the Phase 3 LSTM model."
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the Phase 4 evaluation contract in tests</name>
  <read_first>
    - tests/test_trainer.py
    - tests/test_model.py
    - src/preprocessor.py
    - src/trainer.py
    - .planning/phases/04-evaluation-visualization/04-CONTEXT.md
  </read_first>
  <files>tests/test_evaluator.py</files>
  <behavior>
    - Test 1: Given a training result with an in-memory model and a preprocessing bundle with `X_test`, `y_test`, and `scaler`, `evaluate_model(...)` returns scaled predictions, inverse-transformed USD arrays, metrics, metrics artifact path, and threshold pass/fail state.
    - Test 2: `output/metrics.json` contains RMSE, MAPE, locked thresholds, and pass/fail flags per D-04 and D-05.
    - Test 3: `reload_saved_model_smoke_test(...)` lazy-loads `output/best_model.h5`, runs smoke inference on test inputs, and returns proof data per D-02.
    - Test 4: Missing TensorFlow during reload raises a clear runtime error instead of failing at module import time.
    - Test 5: Missing model/scaler/test tensors raise hard errors per D-09.
  </behavior>
  <action>Write `tests/test_evaluator.py` first. Mirror the existing Phase 3 contract-test style with fake models, fake scalers, monkeypatched TensorFlow modules, and temp directories. Lock these exact behaviors: use the in-memory training-result model as the primary inference path per D-01; inverse-transform predictions and actuals back to USD per D-02; write `output/metrics.json` with metric values plus `rmse_target`, `mape_target`, `rmse_pass`, and `mape_pass` fields per D-04/D-05; treat missing artifacts or invalid evaluation inputs as exceptions per D-09; keep threshold misses non-fatal per D-10.</action>
  <acceptance_criteria>
    - `tests/test_evaluator.py` contains `def test_evaluate_model_returns_predictions_metrics_and_artifact`.
    - `tests/test_evaluator.py` contains `metrics.json` expectations for raw metrics, thresholds, and pass/fail state.
    - `tests/test_evaluator.py` contains a reload smoke-test covering `output/best_model.h5`.
    - `tests/test_evaluator.py` contains a TensorFlow-unavailable runtime error test.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_evaluator.py -x</automated>
  </verify>
  <done>Failing tests exist that fully specify the evaluator public contract and artifact requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement evaluator helpers, metrics artifact writing, and saved-model smoke test</name>
  <read_first>
    - src/config.py
    - src/preprocessor.py
    - src/trainer.py
    - src/model.py
    - tests/test_evaluator.py
    - .planning/phases/04-evaluation-visualization/04-CONTEXT.md
  </read_first>
  <files>src/config.py, src/evaluator.py</files>
  <behavior>
    - Test 1: Evaluator returns a structured bundle with prediction arrays, metrics, artifact path, and reload-proof metadata.
    - Test 2: Metrics artifact persists concise JSON with threshold pass/fail markers instead of diagnostics spam.
    - Test 3: TensorFlow load-model import stays localized to the reload helper.
  </behavior>
  <action>Add `METRICS_FILE = "metrics.json"`, `RMSE_TARGET = 5.0`, and `MAPE_TARGET = 5.0` to `src/config.py`. Create `src/evaluator.py` with concrete public helpers `evaluate_model(training_result, preprocessing_bundle, cfg=None)`, `reload_saved_model_smoke_test(preprocessing_bundle, training_result, cfg=None)`, and `format_evaluation_summary(result)`. In `evaluate_model(...)`, require `training_result['model']`, `training_result['checkpoint_path']`, `preprocessing_bundle['X_test']`, `preprocessing_bundle['y_test']`, and `preprocessing_bundle['scaler']`; run `model.predict(X_test, verbose=0)`; reshape predictions and targets to `(n, 1)` before inverse-transform; compute RMSE in USD and MAPE percentage; write `output/metrics.json`; and return a bundle containing prediction arrays, actual arrays, metrics values, thresholds, pass/fail state, metrics path, and checkpoint path. In `reload_saved_model_smoke_test(...)`, lazy-import `tensorflow.keras.models.load_model`, load `training_result['checkpoint_path']`, run `predict` against `X_test[:1]`, and return concise proof data. Raise explicit runtime/value errors for missing model artifacts, missing scaler/tensors, failed inverse-transform, or write failures per D-09. Do not raise when thresholds fail; instead, mark `rmse_pass`/`mape_pass` false per D-10.</action>
  <acceptance_criteria>
    - `src/config.py` contains `METRICS_FILE = "metrics.json"`.
    - `src/config.py` contains `RMSE_TARGET = 5.0`.
    - `src/config.py` contains `MAPE_TARGET = 5.0`.
    - `src/evaluator.py` exports `evaluate_model`, `reload_saved_model_smoke_test`, and `format_evaluation_summary`.
    - `src/evaluator.py` writes `output/metrics.json` and includes threshold pass/fail state.
    - `src/evaluator.py` lazy-loads TensorFlow only inside the reload helper.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_evaluator.py tests/test_trainer.py tests/test_model.py -x</automated>
  </verify>
  <done>Phase 4 has a reusable evaluation module that satisfies the new contract tests and preserves Phase 3 lazy-import conventions.</done>
</task>

</tasks>

<verification>
Run the evaluator test suite and confirm `output/metrics.json` is produced by the contract tests with threshold status fields instead of a verbose diagnostics blob.
</verification>

<success_criteria>
- Predictions are generated from the in-memory trained model.
- Predictions and actuals are inverse-transformed to USD before metric calculation.
- `output/metrics.json` stores RMSE, MAPE, target thresholds, and pass/fail state.
- Saved-model reuse is provable through a reload smoke-test helper.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evaluation-visualization/04-evaluation-visualization-01-SUMMARY.md`
</output>
