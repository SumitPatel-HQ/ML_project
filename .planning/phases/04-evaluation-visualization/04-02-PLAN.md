---
phase: 04-evaluation-visualization
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/visualizer.py
  - main.py
  - tests/test_visualizer_predictions.py
  - tests/test_main_phase4.py
autonomous: true
requirements:
  - EVAL-05
  - EVAL-06
must_haves:
  truths:
    - User sees a saved `output/AAPL_prediction.png` comparison chart with actual and predicted prices.
    - User sees Phase 4 CLI output that reports RMSE, MAPE, threshold pass/fail state, and saved artifact paths.
    - User can tell from the chart title and CLI output whether the model hit or missed the targets.
  artifacts:
    - path: src/visualizer.py
      provides: Prediction plot helper using locked title and axis labels.
      exports:
        - plot_predictions
    - path: main.py
      provides: Phase 4 pipeline orchestration and concise evaluation proof output.
    - path: tests/test_visualizer_predictions.py
      provides: Locked chart-label/title/output-path coverage.
    - path: tests/test_main_phase4.py
      provides: Phase 4 CLI wiring coverage.
  key_links:
    - from: main.py
      to: src.evaluator.evaluate_model
      via: post-training Phase 4 execution
      pattern: evaluate_model
    - from: main.py
      to: src.visualizer.plot_predictions
      via: evaluation bundle actual/prediction arrays
      pattern: plot_predictions
    - from: src.visualizer.py
      to: output/AAPL_prediction.png
      via: matplotlib savefig
      pattern: AAPL_prediction.png
---

<objective>
Wire the Phase 4 evaluation results into the user-facing pipeline so the app prints a concise metrics summary and saves the final actual-vs-predicted plot.

Purpose: This completes the user-visible Phase 4 experience on top of the evaluator contract from Plan 04-01.
Output: Plot helper, Phase 4 CLI wiring, and regression tests for plot + pipeline output.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-evaluation-visualization/04-CONTEXT.md
@.planning/phases/04-evaluation-visualization/04-RESEARCH.md
@.planning/phases/03-model-architecture-training/03-03-SUMMARY.md
@main.py
@src/visualizer.py
@src/evaluator.py

<interfaces>
From `src.evaluator` (created in 04-01):
```python
def evaluate_model(training_result, preprocessing_bundle, cfg=None):
    ...

def reload_saved_model_smoke_test(preprocessing_bundle, training_result, cfg=None):
    ...

def format_evaluation_summary(result):
    ...
```

From existing `main.py`:
```python
bundle = preprocess(df)
training_result = train_model(model, bundle)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the prediction-plot contract in tests and extend the visualizer</name>
  <read_first>
    - src/visualizer.py
    - tests/test_console_output.py
    - .planning/phases/04-evaluation-visualization/04-CONTEXT.md
    - .docs/TDD_StockPrediction_LSTM.md
  </read_first>
  <files>tests/test_visualizer_predictions.py, src/visualizer.py</files>
  <behavior>
    - Test 1: `plot_predictions(...)` saves `output/AAPL_prediction.png` by default.
    - Test 2: The plot uses actual blue solid and predicted orange dashed lines per D-06.
    - Test 3: The title includes RMSE and MAPE, the x-axis is `Trading Days (Test Set)`, and the y-axis is `Close Price (USD)` per D-07 and D-08.
  </behavior>
  <action>Create `tests/test_visualizer_predictions.py` first, then extend `src/visualizer.py` with a new `plot_predictions(actual_prices, predicted_prices, rmse, mape, output_filename=PREDICTION_PLOT_FILE, ticker="AAPL")` helper. Reuse the existing matplotlib save-to-file pattern and explicit `plt.close(fig)`. Make the actual series blue solid, predicted series orange dashed, title include RMSE and MAPE, x-axis exactly `Trading Days (Test Set)`, and y-axis exactly `Close Price (USD)` per D-06/D-07/D-08. Keep console output ASCII-safe and concise.</action>
  <acceptance_criteria>
    - `tests/test_visualizer_predictions.py` contains axis-label expectations for `Trading Days (Test Set)` and `Close Price (USD)`.
    - `src/visualizer.py` exports `plot_predictions`.
    - `src/visualizer.py` references `PREDICTION_PLOT_FILE` for the default output filename.
    - `src/visualizer.py` title formatting includes both `RMSE` and `MAPE` strings.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_visualizer_predictions.py tests/test_console_output.py -x</automated>
  </verify>
  <done>The visualizer can produce the locked prediction chart and its behavior is pinned by automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Phase 4 evaluator output and plot generation into main.py</name>
  <read_first>
    - main.py
    - src/evaluator.py
    - src/visualizer.py
    - tests/test_main_phase3.py
    - .planning/phases/04-evaluation-visualization/04-CONTEXT.md
  </read_first>
  <files>tests/test_main_phase4.py, main.py</files>
  <behavior>
    - Test 1: `main.py` prints a Phase 4 banner and concise evaluation summary instead of arrays per D-03.
    - Test 2: `main.py` calls the Phase 4 evaluator using the training-result bundle as the primary path per D-01.
    - Test 3: `main.py` triggers the saved-model smoke-test per D-02 and saves the prediction plot plus metrics artifact paths in final output.
    - Test 4: Threshold misses remain reported but do not block completion per D-10.
  </behavior>
  <action>Write `tests/test_main_phase4.py` first, following the monkeypatch-heavy style already used in `tests/test_main_phase2.py` and `tests/test_main_phase3.py`. Then update `main.py` to import `evaluate_model`, `reload_saved_model_smoke_test`, and `format_evaluation_summary` from `src.evaluator`, plus `plot_predictions` from `src.visualizer`. Add a new Phase 4 block after successful Phase 3 training: call `evaluate_model(training_result, bundle)`, print `format_evaluation_summary(result)` as concise proof per D-03, call `reload_saved_model_smoke_test(bundle, training_result)` to prove reuse per D-02, then call `plot_predictions(result['actual_usd'], result['predictions_usd'], rmse=result['metrics']['rmse'], mape=result['metrics']['mape'])`. Keep D-09 behavior by letting missing artifacts or failed inference raise immediately. Update the final output section to list the prediction plot path and metrics artifact path in addition to the existing model/training artifacts.</action>
  <acceptance_criteria>
    - `tests/test_main_phase4.py` contains `PHASE 4: Evaluation & Visualization` output assertions.
    - `main.py` imports `evaluate_model`, `reload_saved_model_smoke_test`, and `format_evaluation_summary`.
    - `main.py` imports and calls `plot_predictions`.
    - `main.py` prints both prediction plot and metrics artifact paths in the final summary.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_main_phase2.py tests/test_main_phase3.py tests/test_main_phase4.py tests/test_visualizer_predictions.py -x</automated>
  </verify>
  <done>The CLI pipeline now runs Phase 4, prints concise evaluation proof, saves the locked plot artifact, and preserves earlier phase regressions.</done>
</task>

</tasks>

<verification>
Run the full Phase 2-4 CLI regression tests and confirm the pipeline surfaces metrics, metrics artifact path, and prediction plot path without breaking earlier proof output.
</verification>

<success_criteria>
- `output/AAPL_prediction.png` is produced by the Phase 4 plot helper.
- Plot title includes RMSE and MAPE.
- CLI output includes Phase 4 banner, concise metrics summary, and artifact paths.
- Threshold misses are reported without suppressing artifact generation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evaluation-visualization/04-evaluation-visualization-02-SUMMARY.md`
</output>
