---
phase: 05-autonomous-correction-performance-optimization-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/config.py
  - src/repair_loop.py
  - tests/test_repair_loop.py
autonomous: true
requirements:
  - AUTO-02
  - AUTO-03
  - AUTO-04
must_haves:
  truths:
    - Failed verification triggers diagnosis-driven repair attempts without human intervention.
    - A repair that worsens metrics or breaks the pipeline is automatically reverted to the last fully passing baseline per D-05 and D-06.
    - `REPAIR-LOG.md` records each attempt with rationale plus before/after metrics per D-07 and D-08.
    - Autonomous success requires 3 consecutive passing runs and stops when the attempt/time budget is exhausted per D-09 and D-10.
  artifacts:
    - path: src/repair_loop.py
      provides: Autonomous repair controller, rollback helpers, subprocess runner, repair log writer, and result formatter.
      exports:
        - run_autonomous_repair_loop
        - run_pipeline_subprocess
        - write_repair_log
        - format_repair_outcome
    - path: tests/test_repair_loop.py
      provides: Contract coverage for pass streak handling, rollback, repair budgets, and REPAIR-LOG.md structure.
    - path: src/config.py
      provides: Stable repair-loop budget and log-path constants.
      contains: AUTONOMOUS_PASS_STREAK
  key_links:
    - from: src/repair_loop.py
      to: src/autonomous_verifier.py
      via: verification reports and diagnosis categories
      pattern: diagnose_verification_failure
    - from: src/repair_loop.py
      to: workspace files
      via: snapshot and restore of changed files before/after repairs
      pattern: changed_files
    - from: src/repair_loop.py
      to: REPAIR-LOG.md
      via: markdown writer with per-attempt entries and final summary
      pattern: REPAIR-LOG.md
---

<objective>
Create the autonomous repair controller so Phase 5 can retry runs, revert bad fixes, enforce pass-streak and budget rules, and document the whole loop in `REPAIR-LOG.md`.

Purpose: This is the self-healing engine consumed by the Phase 5 CLI entrypoint.
Output: `src/repair_loop.py`, repair-loop config constants, and passing controller tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-autonomous-correction-performance-optimization-loop/05-CONTEXT.md
@.planning/phases/5-autonomous-correction-performance-optimization-loop/PHASE.md
@.planning/phases/05-autonomous-correction-performance-optimization-loop/05-01-PLAN.md
@src/config.py
@src/autonomous_verifier.py

<interfaces>
From `src/autonomous_verifier.py`:
```python
def build_autonomous_verification_report(run_result, cfg=None): ...
def diagnose_verification_failure(report, cfg=None): ...
```

Expected repair-loop result shape:
```python
{
    "status": "passed" | "unresolved",
    "consecutive_passes": 0,
    "repair_attempts": 0,
    "repair_log_path": "REPAIR-LOG.md",
    "final_report": {...},
    "final_diagnosis": {...},
}
```

Expected repair-action result shape:
```python
{
    "name": "attempt-1-lower-learning-rate",
    "rationale": "Reduce overshoot after metric regression.",
    "changed_files": ["src/config.py"],
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the repair-loop and rollback contract in tests</name>
  <read_first>
    - tests/test_trainer.py
    - src/config.py
    - src/autonomous_verifier.py
    - .planning/phases/05-autonomous-correction-performance-optimization-loop/05-CONTEXT.md
  </read_first>
  <files>tests/test_repair_loop.py</files>
  <behavior>
    - Test 1: Three consecutive passing reports are required before the loop returns `status="passed"`, per D-09.
    - Test 2: A failed run triggers diagnosis and one repair attempt at a time until the `AUTONOMOUS_MAX_REPAIR_ATTEMPTS` or `AUTONOMOUS_MAX_REPAIR_MINUTES` budget is exceeded, per D-10.
    - Test 3: If a repair worsens RMSE/MAPE or turns a passing run into a runtime failure, changed files are restored automatically to the last fully passing baseline, per D-05 and D-06.
    - Test 4: `REPAIR-LOG.md` uses hybrid logging: attempt-by-attempt entries plus a final summary including before/after metrics and rationale, per D-07 and D-08.
  </behavior>
  <action>Write `tests/test_repair_loop.py` first using fake verification reports, fake diagnosis helpers, fake repair callbacks, and temp workspace files. Lock these exact behaviors: `run_autonomous_repair_loop(...)` must require 3 consecutive passing reports before success per D-09; it must stop after 2 repair attempts or 10 elapsed minutes per D-10; it must snapshot each file listed in `repair_action["changed_files"]` before the repair runs and restore those files automatically when the post-repair report is worse than the last fully passing baseline per D-05/D-06; and it must write `REPAIR-LOG.md` entries containing attempt number, diagnosis category, rationale, changed files, before metrics, after metrics, and final outcome per D-07/D-08.</action>
  <acceptance_criteria>
    - `tests/test_repair_loop.py` contains `def test_run_autonomous_repair_loop_requires_three_consecutive_passes`.
    - `tests/test_repair_loop.py` contains rollback assertions against a temp file restored to its original contents.
    - `tests/test_repair_loop.py` contains a budget-stop test for `2` attempts or `10` minutes.
    - `tests/test_repair_loop.py` contains assertions for `REPAIR-LOG.md` attempt entries and final summary content.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_repair_loop.py -x</automated>
  </verify>
  <done>Failing tests exist that fully define pass streak, rollback, budget, and repair-log behavior for the autonomous controller.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the autonomous repair controller, subprocess runner, and log writer</name>
  <read_first>
    - src/config.py
    - src/autonomous_verifier.py
    - tests/test_repair_loop.py
    - main.py
    - .planning/phases/05-autonomous-correction-performance-optimization-loop/05-CONTEXT.md
  </read_first>
  <files>src/config.py, src/repair_loop.py</files>
  <behavior>
    - Test 1: Controller loops until either 3 consecutive passes succeed or the budget expires.
    - Test 2: Snapshot/restore protects the last fully passing baseline from bad repairs.
    - Test 3: Repair log persists hybrid markdown evidence with rationale and before/after metrics.
  </behavior>
  <action>Add these concrete constants to `src/config.py`: `AUTONOMOUS_PASS_STREAK = 3`, `AUTONOMOUS_MAX_REPAIR_ATTEMPTS = 2`, `AUTONOMOUS_MAX_REPAIR_MINUTES = 10`, and `REPAIR_LOG_FILE = "REPAIR-LOG.md"`. Create `src/repair_loop.py` with public helpers `run_pipeline_subprocess(command, env=None, cwd=None, timeout=None)`, `run_autonomous_repair_loop(initial_report, rerun_pipeline, verify_run, diagnose_failure, apply_repair, cfg=None, now_fn=None)`, `write_repair_log(log_path, attempts, final_summary)`, and `format_repair_outcome(result)`. `run_autonomous_repair_loop(...)` must treat the last report satisfying D-04 as the rollback baseline per D-06; when `apply_repair(...)` returns a mapping with `name`, `rationale`, and `changed_files`, snapshot those files before edits so the controller can restore them automatically if the next report degrades or crashes per D-05; track consecutive passing runs until `AUTONOMOUS_PASS_STREAK == 3` per D-09; stop once `AUTONOMOUS_MAX_REPAIR_ATTEMPTS == 2` or elapsed minutes exceed `10` per D-10; and always write `REPAIR-LOG.md` with concise per-attempt sections plus a terminal summary per D-07/D-08. Build the markdown log with exact headings `## Attempt 1`, `## Attempt 2`, and `## Final Outcome` so downstream verification can grep predictable structure. Keep the repair interface generic enough for broad code edits per D-01 by restoring arbitrary files from `changed_files`, not just `src/config.py`.</action>
  <acceptance_criteria>
    - `src/config.py` contains `AUTONOMOUS_PASS_STREAK = 3`.
    - `src/config.py` contains `AUTONOMOUS_MAX_REPAIR_ATTEMPTS = 2`.
    - `src/config.py` contains `AUTONOMOUS_MAX_REPAIR_MINUTES = 10`.
    - `src/config.py` contains `REPAIR_LOG_FILE = "REPAIR-LOG.md"`.
    - `src/repair_loop.py` exports `run_pipeline_subprocess`, `run_autonomous_repair_loop`, `write_repair_log`, and `format_repair_outcome`.
    - `src/repair_loop.py` writes markdown headings `## Attempt 1` and `## Final Outcome`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_repair_loop.py tests/test_autonomous_verifier.py -x</automated>
  </verify>
  <done>Phase 5 has a rollback-safe repair engine that can retry, revert, and log autonomous fixes using the locked stop conditions.</done>
</task>

</tasks>

<verification>
Run the repair-loop tests and confirm a bad repair restores the baseline file contents while `REPAIR-LOG.md` captures attempt evidence and final outcome.
</verification>

<success_criteria>
- Autonomous control requires 3 consecutive passes before success.
- Bad repairs are reverted automatically to the last fully passing baseline.
- `REPAIR-LOG.md` records before/after metrics and rationale for every attempt.
- Locked stop conditions from D-05 through D-10 are enforced in code and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-autonomous-correction-performance-optimization-loop/05-autonomous-correction-performance-optimization-loop-02-SUMMARY.md`
</output>
