---
phase: 05-autonomous-correction-performance-optimization-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/autonomous_verifier.py
  - tests/test_autonomous_verifier.py
autonomous: true
requirements:
  - AUTO-01
  - AUTO-02
must_haves:
  truths:
    - Agent can judge a full pipeline run by clean exit, metric thresholds, and required output artifacts.
    - Agent can detect both deterministic failure injection and natural regressions per D-03.
    - Agent can classify failures into actionable diagnosis buckets without violating offline-only, AAPL-only, Close-only, stacked-LSTM constraints per D-02.
  artifacts:
    - path: src/autonomous_verifier.py
      provides: Autonomous verification report, diagnosis helper, and summary formatter for Phase 5.
      exports:
        - build_autonomous_verification_report
        - diagnose_verification_failure
        - format_autonomous_verification_summary
    - path: tests/test_autonomous_verifier.py
      provides: Contract coverage for passing runs, injected failures, runtime failures, and missing-artifact detection.
    - path: src/config.py
      provides: Stable Phase 5 verification constants and failure-injection env var names.
      contains: AUTONOMOUS_FAILURE_ENV
  key_links:
    - from: src/autonomous_verifier.py
      to: output/metrics.json
      via: JSON metric and threshold inspection
      pattern: metrics.json
    - from: src/autonomous_verifier.py
      to: output/best_model.h5 and output/AAPL_prediction.png
      via: required artifact existence checks
      pattern: best_model.h5
    - from: src/autonomous_verifier.py
      to: main.py / subprocess runner output
      via: run_result exit code, stdout, stderr, and failure injection metadata
      pattern: exit_code
---

<objective>
Create the Phase 5 verification-and-diagnosis contract so autonomous repair work can evaluate a run, detect forced or natural regressions, and explain what failed before any repair logic starts.

Purpose: This is the Phase 5 foundation consumed by the repair controller and CLI wiring.
Output: `src/autonomous_verifier.py`, Phase 5 verification constants, and passing contract tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-autonomous-correction-performance-optimization-loop/05-CONTEXT.md
@.planning/phases/5-autonomous-correction-performance-optimization-loop/PHASE.md
@.planning/phases/03-model-architecture-training/03-02-SUMMARY.md
@.planning/phases/03-model-architecture-training/03-03-SUMMARY.md
@.planning/phases/04-evaluation-visualization/04-evaluation-visualization-01-SUMMARY.md
@.planning/phases/04-evaluation-visualization/04-evaluation-visualization-02-SUMMARY.md
@main.py
@src/config.py
@src/evaluator.py

<interfaces>
From `src/evaluator.py`:
```python
def evaluate_model(training_result, preprocessing_bundle, cfg=None):
    return {
        "metrics": metrics,
        "thresholds": thresholds,
        "passes": passes,
        "metrics_path": metrics_path,
        "checkpoint_path": checkpoint_path,
        "predictions_usd": predictions_usd,
        "actual_usd": actual_usd,
    }
```

From `main.py`:
```python
evaluation_result = {"metrics_path": "not-run"}
prediction_plot_path = "not-run"
if "model" in training_result and "X_test" in bundle:
    evaluation_result = evaluate_model(training_result, bundle)
    reload_saved_model_smoke_test(bundle, training_result)
    prediction_plot_path = plot_predictions(...)
```

From `src/config.py`:
```python
MODEL_FILE = "best_model.h5"
METRICS_FILE = "metrics.json"
PREDICTION_PLOT_FILE = "AAPL_prediction.png"
RMSE_TARGET = 5.0
MAPE_TARGET = 5.0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the autonomous verification contract in tests</name>
  <read_first>
    - tests/test_evaluator.py
    - tests/test_main_phase4.py
    - src/evaluator.py
    - src/config.py
    - .planning/phases/05-autonomous-correction-performance-optimization-loop/05-CONTEXT.md
  </read_first>
  <files>tests/test_autonomous_verifier.py</files>
  <behavior>
    - Test 1: A clean run with `exit_code == 0`, passing thresholds, and existing `output/best_model.h5`, `output/metrics.json`, and `output/AAPL_prediction.png` returns `passed=True` per D-04.
    - Test 2: Deterministic failure injection marks the report failed even when the metrics artifact would otherwise pass, per D-03.
    - Test 3: Missing artifacts or non-zero exit codes are diagnosed as explicit runtime or artifact failures instead of ambiguous metric misses, per D-04.
    - Test 4: Diagnosis output returns concrete categories and ranked repair hints while preserving offline-only, AAPL-only, Close-only, stacked-LSTM boundaries per D-02.
  </behavior>
  <action>Write `tests/test_autonomous_verifier.py` first. Mirror the existing Phase 4 contract-test style with temp directories and tiny JSON fixtures. Lock these exact behaviors: `build_autonomous_verification_report(...)` must accept a `run_result` mapping with `exit_code`, `stdout`, `stderr`, `duration_seconds`, `metrics_path`, `checkpoint_path`, `prediction_plot_path`, and optional `failure_injection`; a passing report only occurs when the run exited cleanly, `rmse_pass` and `mape_pass` are both true, and all three expected artifacts exist per D-04; injected `failure_injection="metrics"` and `failure_injection="runtime"` must force failure per D-03; `diagnose_verification_failure(...)` must emit categories from this closed set: `metric_regression`, `runtime_failure`, `artifact_missing`, `environment_blocker`, `injected_failure`; and diagnosis hints must never recommend changing the offline-only, AAPL-only, Close-only, stacked-LSTM baseline per D-02.</action>
  <acceptance_criteria>
    - `tests/test_autonomous_verifier.py` contains `def test_build_autonomous_verification_report_marks_clean_run_as_pass`.
    - `tests/test_autonomous_verifier.py` contains assertions for `failure_injection="metrics"` and `failure_injection="runtime"`.
    - `tests/test_autonomous_verifier.py` contains diagnosis expectations for `metric_regression`, `runtime_failure`, and `artifact_missing`.
    - `tests/test_autonomous_verifier.py` asserts that diagnosis hints retain the offline/AAPL/Close/stacked-LSTM constraints.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_autonomous_verifier.py -x</automated>
  </verify>
  <done>Failing tests exist that completely define the verification and diagnosis contract for autonomous repair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the autonomous verification report and diagnosis helpers</name>
  <read_first>
    - src/config.py
    - src/evaluator.py
    - main.py
    - tests/test_autonomous_verifier.py
    - .planning/phases/05-autonomous-correction-performance-optimization-loop/05-CONTEXT.md
  </read_first>
  <files>src/config.py, src/autonomous_verifier.py</files>
  <behavior>
    - Test 1: Verification helper reads `metrics.json`, checks locked thresholds, and returns a structured pass/fail report.
    - Test 2: Failure injection and natural runtime/artifact failures are differentiated cleanly.
    - Test 3: Diagnosis helper emits categories plus concrete repair hints that keep the locked project boundaries intact.
  </behavior>
  <action>Add the following concrete Phase 5 constants to `src/config.py`: `AUTONOMOUS_FAILURE_ENV = "LSTM_FORCE_AUTONOMOUS_FAILURE"`, `AUTONOMOUS_REQUIRED_ARTIFACTS = (MODEL_FILE, METRICS_FILE, PREDICTION_PLOT_FILE)`, and `AUTONOMOUS_ALLOWED_REPAIR_SCOPE = ("offline-only", "AAPL-only", "Close-only", "stacked-LSTM")`. Create `src/autonomous_verifier.py` with public helpers `build_autonomous_verification_report(run_result, cfg=None)`, `diagnose_verification_failure(report, cfg=None)`, and `format_autonomous_verification_summary(report)`. `build_autonomous_verification_report(...)` must read the metrics artifact from `run_result["metrics_path"]`, require `exit_code == 0`, require all three artifact paths to exist, preserve `rmse`, `mape`, pass flags, and duration in the returned report, and force failure when `run_result["failure_injection"]` is `"metrics"` or `"runtime"` per D-03/D-04. `diagnose_verification_failure(...)` must map failures to the closed category set from Task 1 and return ranked hints with exact target surfaces such as `src/config.py` hyperparameters, missing artifact regeneration, or environment blockers; explicitly note that broad code edits are allowed per D-01 while the offline/AAPL/Close/stacked-LSTM contract remains locked per D-02. Do not import TensorFlow here; Phase 5 verification must stay lightweight and inspect artifacts plus process results only.</action>
  <acceptance_criteria>
    - `src/config.py` contains `AUTONOMOUS_FAILURE_ENV = "LSTM_FORCE_AUTONOMOUS_FAILURE"`.
    - `src/config.py` contains `AUTONOMOUS_REQUIRED_ARTIFACTS = (`.
    - `src/autonomous_verifier.py` exports `build_autonomous_verification_report`, `diagnose_verification_failure`, and `format_autonomous_verification_summary`.
    - `src/autonomous_verifier.py` reads `metrics.json` and checks artifact existence.
    - `src/autonomous_verifier.py` handles `failure_injection` values `metrics` and `runtime`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_autonomous_verifier.py tests/test_evaluator.py tests/test_main_phase4.py -x</automated>
  </verify>
  <done>Phase 5 has a reusable, artifact-driven verification contract that can judge runs and explain failures before repair logic begins.</done>
</task>

</tasks>

<verification>
Run the new verifier tests and confirm the report only passes when exit code, metrics thresholds, and required artifacts all succeed together.
</verification>

<success_criteria>
- Phase 5 can evaluate a run from process results plus saved artifacts.
- Deterministic failure injection and natural regressions are both detectable.
- Failure diagnosis categories are explicit and bounded.
- Locked Phase 5 constraints from D-01 through D-04 are represented in code and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-autonomous-correction-performance-optimization-loop/05-autonomous-correction-performance-optimization-loop-01-SUMMARY.md`
</output>
