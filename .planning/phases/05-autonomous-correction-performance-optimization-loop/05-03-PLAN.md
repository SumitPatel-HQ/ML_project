---
phase: 05-autonomous-correction-performance-optimization-loop
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - main.py
  - tests/test_main_phase5.py
autonomous: true
requirements:
  - AUTO-01
  - AUTO-02
  - AUTO-03
  - AUTO-04
must_haves:
  truths:
    - User can run `python main.py` and see a Phase 5 autonomous verification/repair summary after Phase 4 completes.
    - Phase 5 reruns the full pipeline without recursive self-invocation by guarding child runs.
    - User sees `REPAIR-LOG.md` path and terminal autonomous status in the CLI output.
  artifacts:
    - path: main.py
      provides: Phase 5 entrypoint, recursion guard, and proof-style autonomous summary output.
    - path: tests/test_main_phase5.py
      provides: CLI contract coverage for Phase 5 status, repair log reporting, and rerun guard wiring.
  key_links:
    - from: main.py
      to: src.autonomous_verifier.build_autonomous_verification_report
      via: current-run artifact paths and injected failure mode metadata
      pattern: build_autonomous_verification_report
    - from: main.py
      to: src.repair_loop.run_autonomous_repair_loop
      via: initial report plus child rerun callback
      pattern: run_autonomous_repair_loop
    - from: main.py
      to: child `python main.py` process
      via: `LSTM_SKIP_PHASE5=1` environment guard
      pattern: LSTM_SKIP_PHASE5
---

<objective>
Wire Phase 5 into `main.py` so the existing offline pipeline can launch the autonomous verification/repair loop, rerun itself safely, and print proof-oriented terminal output.

Purpose: This delivers the user-visible Phase 5 experience and closes the loop on autonomous verification, diagnosis, repair, and logging.
Output: Updated `main.py` Phase 5 flow and passing CLI integration tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-autonomous-correction-performance-optimization-loop/05-CONTEXT.md
@.planning/phases/05-autonomous-correction-performance-optimization-loop/05-01-PLAN.md
@.planning/phases/05-autonomous-correction-performance-optimization-loop/05-02-PLAN.md
@main.py
@src/config.py
@src/autonomous_verifier.py
@src/repair_loop.py
@tests/test_main_phase4.py

<interfaces>
From `src/autonomous_verifier.py`:
```python
def build_autonomous_verification_report(run_result, cfg=None): ...
def format_autonomous_verification_summary(report): ...
```

From `src/repair_loop.py`:
```python
def run_pipeline_subprocess(command, env=None, cwd=None, timeout=None): ...
def run_autonomous_repair_loop(initial_report, rerun_pipeline, verify_run, diagnose_failure, apply_repair, cfg=None, now_fn=None): ...
def format_repair_outcome(result): ...
```

Current Phase 4 output block in `main.py`:
```python
print(f"  - Metrics artifact: {evaluation_result['metrics_path']}")
print(f"  - Prediction plot: {prediction_plot_path}")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the Phase 5 CLI integration contract in tests</name>
  <read_first>
    - tests/test_main_phase4.py
    - main.py
    - src/autonomous_verifier.py
    - src/repair_loop.py
    - .planning/phases/05-autonomous-correction-performance-optimization-loop/05-CONTEXT.md
  </read_first>
  <files>tests/test_main_phase5.py</files>
  <behavior>
    - Test 1: After Phase 4 evaluation finishes, `main.py` prints a `PHASE 5: Autonomous Correction & Performance Optimization Loop` banner and a terminal status line.
    - Test 2: `main.py` passes the current run's exit code, metrics path, checkpoint path, prediction plot path, and injected failure mode to `build_autonomous_verification_report(...)`, per D-03 and D-04.
    - Test 3: Child reruns always set `LSTM_SKIP_PHASE5=1` so the subprocess executes only Phases 1-4 and avoids recursive Phase 5 invocation.
    - Test 4: Final CLI output includes the repair log path and the formatted repair outcome.
  </behavior>
  <action>Write `tests/test_main_phase5.py` first using the same monkeypatch-heavy integration style as `tests/test_main_phase4.py`. Lock these exact behaviors: the main pipeline must call `build_autonomous_verification_report(...)` with `exit_code=0`, `metrics_path=evaluation_result["metrics_path"]`, `checkpoint_path=training_result["checkpoint_path"]`, `prediction_plot_path=prediction_plot_path`, and `failure_injection=os.getenv("LSTM_FORCE_AUTONOMOUS_FAILURE", "")` per D-03/D-04; it must call `run_autonomous_repair_loop(...)` after Phase 4; the child rerun callback passed into the repair loop must use `LSTM_SKIP_PHASE5=1`; and the console output must contain `Autonomous status:`, `Repair log: REPAIR-LOG.md`, and the Phase 5 banner.</action>
  <acceptance_criteria>
    - `tests/test_main_phase5.py` contains `def test_main_prints_phase5_autonomous_summary_and_repair_log_path`.
    - `tests/test_main_phase5.py` asserts `failure_injection` is passed from `os.getenv("LSTM_FORCE_AUTONOMOUS_FAILURE", "")`.
    - `tests/test_main_phase5.py` asserts child reruns include `LSTM_SKIP_PHASE5=1`.
    - `tests/test_main_phase5.py` asserts CLI output contains `Autonomous status:` and `Repair log: REPAIR-LOG.md`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_main_phase5.py -x</automated>
  </verify>
  <done>Failing integration tests exist that define the full Phase 5 CLI behavior and recursion guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Phase 5 autonomous verification and repair flow into main.py</name>
  <read_first>
    - main.py
    - src/config.py
    - src/autonomous_verifier.py
    - src/repair_loop.py
    - tests/test_main_phase5.py
    - .planning/phases/05-autonomous-correction-performance-optimization-loop/05-CONTEXT.md
  </read_first>
  <files>main.py</files>
  <behavior>
    - Test 1: Main pipeline launches Phase 5 only in top-level runs.
    - Test 2: Current-run artifacts become the initial autonomous verification report.
    - Test 3: CLI output shows terminal autonomous status and repair log location.
  </behavior>
  <action>Modify `main.py` to import `os`, `sys`, `build_autonomous_verification_report`, `diagnose_verification_failure`, `format_autonomous_verification_summary`, `run_pipeline_subprocess`, `run_autonomous_repair_loop`, and `format_repair_outcome`. Add a recursion guard: if `os.getenv("LSTM_SKIP_PHASE5") == "1"`, skip the Phase 5 section entirely so child reruns stop after Phase 4. Otherwise, after the existing Phase 4 block, print a new `PHASE 5: Autonomous Correction & Performance Optimization Loop` banner, build `initial_run_result = {"exit_code": 0, "stdout": "", "stderr": "", "duration_seconds": 0.0, "metrics_path": evaluation_result["metrics_path"], "checkpoint_path": training_result["checkpoint_path"], "prediction_plot_path": prediction_plot_path, "failure_injection": os.getenv("LSTM_FORCE_AUTONOMOUS_FAILURE", "")}` when Phase 4 ran, and build the same shape with a non-zero exit code plus `phase4_skipped` text when training/evaluation prerequisites were missing. Feed that mapping into `build_autonomous_verification_report(...)`, pass the report into `run_autonomous_repair_loop(...)`, and give the repair loop a child runner that calls `run_pipeline_subprocess([sys.executable, "main.py"], env={**os.environ, "LSTM_SKIP_PHASE5": "1"})`. Print the formatted verification summary, then print exact proof lines `Autonomous status: ...` and `Repair log: REPAIR-LOG.md`, followed by `format_repair_outcome(...)`. Keep all output ascii-safe to match the Windows console constraint from earlier phases.</action>
  <acceptance_criteria>
    - `main.py` contains `if os.getenv("LSTM_SKIP_PHASE5") == "1"`.
    - `main.py` contains the banner text `PHASE 5: Autonomous Correction & Performance Optimization Loop`.
    - `main.py` contains `failure_injection": os.getenv("LSTM_FORCE_AUTONOMOUS_FAILURE", "")`.
    - `main.py` contains `LSTM_SKIP_PHASE5": "1"` in the child rerun environment.
    - `main.py` prints `Autonomous status:` and `Repair log: REPAIR-LOG.md`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_main_phase5.py tests/test_main_phase4.py tests/test_repair_loop.py tests/test_autonomous_verifier.py -x</automated>
  </verify>
  <done>`main.py` exposes a full Phase 5 autonomous loop entrypoint with safe reruns, proof-oriented output, and repair-log reporting.</done>
</task>

</tasks>

<verification>
Run the new Phase 5 CLI integration test plus the underlying repair/verifier suites and confirm the child rerun path is guarded with `LSTM_SKIP_PHASE5=1`.
</verification>

<success_criteria>
- `python main.py` has a visible Phase 5 section after evaluation.
- The top-level process hands current-run artifacts into autonomous verification.
- Child reruns avoid recursive Phase 5 execution.
- Final CLI output reports autonomous status and `REPAIR-LOG.md` path.
</success_criteria>

<output>
After completion, create `.planning/phases/05-autonomous-correction-performance-optimization-loop/05-autonomous-correction-performance-optimization-loop-03-SUMMARY.md`
</output>
